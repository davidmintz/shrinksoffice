{% macro xhr_status(id = 'status', class = 'alert-success') %}
    <div id="{{ id }}" class="alert {{ class }} d-none mx-auto my-5" data-person-target="status" style="max-width:600px">placeholder text for testing</div>
{% endmacro %}
